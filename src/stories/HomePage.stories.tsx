import { Meta } from "@storybook/react";
import ExamplePage from "../app/(me)/home/page";

const meta = {
  component: ExamplePage,
  title: "Pages/HomePage",
  parameters: {
    layout: "fullscreen",
  },
};
export default meta;

export const Default = {};
