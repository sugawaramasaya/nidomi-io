import { Meta } from "@storybook/react";
import MyPage from "../app/(me)/mypage/page";

const meta = {
  component: MyPage,
  title: "Pages/MyPage",
  parameters: {
    layout: "fullscreen",
  },
};
export default meta;

export const Default = {};
